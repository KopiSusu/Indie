<section class='container' id='desc'>
  <article>
    <h3> "Some Text, Some text that describes the purpose of the site" </h3>
  </article>
</section>

<section class='container' id='games'>

  <% @games.each do |game| %>
    <ul>
      <%= link_to image_tag(game.image.url(:large)), game_path(game) %>
      <h3><%= game.title %></h3>
      <p> "Some review text, some review text, some review text" </p>
    </ul>
  <% end %>

</section>